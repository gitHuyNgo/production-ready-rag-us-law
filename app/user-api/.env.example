# User-api: copy to .env and fill in values.
# Used when running from app/user-api (make server, make test).

USER_DB_URL=mongodb://user-db:27017/user_db
# RS256: public key must match auth-api key pair
JWT_ALGORITHM=RS256
JWT_PUBLIC_KEY_PATH=
JWT_PUBLIC_KEY=
