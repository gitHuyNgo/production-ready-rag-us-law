# Install shared lib first (from repo root): pip install -e libs/code-shared
# Then: pip install -r app/ingestion-worker/requirements.txt

# PDF and chunking; vector store and cache are local to this service
llama-index
docling
weaviate-client
redis
lxml
llama-index-graph-stores-neo4j
neo4j

# Testing
pytest>=7.4.0
pytest-mock>=3.11.0
